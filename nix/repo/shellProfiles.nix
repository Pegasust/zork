# Specifically scoped to conform to nixosModule-like declaration for `numtide/devshells`
# We specifically need this since `divnix/std` does not provide a `devshellProfile`
# blocktype for composibiility from module system
{
  inputs,
  cell,
}: let
in {
}

